# create-rust-app-cli

Thin CLI wrapper for `create-rust-app`. Install it, run `create-rust-app new my-app`,
and pick a template from [cra-templates](https://github.com/Create-Rust-App/cra-templates).
